# 贡献指南

欢迎补充模型专题、论文拆解和可复现案例。提交内容时请遵循以下约定：

1. 新资料放入对应的 `01`–`07` 主题目录，并更新根目录 `README.md` 或 `资料库目录.md`。
2. Markdown 文件使用 UTF-8 编码；标题层级从一个 `#` 开始，内部链接使用相对路径。
3. 模型说明尽量包含适用场景、变量与假设、核心公式、求解流程、检验方法和局限。
4. 数据、论文和图片必须标明来源，并确认允许公开；不要提交个人信息、账号密钥或竞赛禁传资料。
5. 二进制原件尽量同时提供可在 GitHub 直接阅读的 Markdown 说明。

